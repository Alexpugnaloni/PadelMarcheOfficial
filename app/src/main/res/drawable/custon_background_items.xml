
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Colore di riempimento -->
    <stroke
        android:width="2dp"
        android:color="@color/blu" /> <!-- Colore del contorno -->
    <corners android:radius="8dp" /> <!-- Angoli arrotondati -->
    <padding
        android:left="5dp"
        android:right="5dp"
        android:top="5dp"
        android:bottom="5dp" />
</shape>
